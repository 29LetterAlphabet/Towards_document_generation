Some of the data values need to be changed at the beginning of the pandasFilter and plotList files to ensure that the right variable is filtered. These program sets are meant to be run on output from either the pandasJson program or the splitter program. Before report2 is run you should strucutre the latex generation to match the output created by pandasDictPlot as it will sometimes have empty fields.s
